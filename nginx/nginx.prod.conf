# ── satryawiguna.me (web/portfolio) ──────────────────────────────────────────
server {
    listen 80;
    server_name satryawiguna.me www.satryawiguna.me;

    # Uncomment the block below once SSL certs are in place, then
    # redirect HTTP → HTTPS here instead of serving directly.
    # return 301 https://$host$request_uri;

    location / {
        proxy_pass         http://web:3004;
        proxy_http_version 1.1;
        proxy_set_header   Upgrade           $http_upgrade;
        proxy_set_header   Connection        "upgrade";
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 60s;
    }
}

# Uncomment and configure once you have SSL certificates (e.g. Let's Encrypt)
# server {
#     listen 443 ssl http2;
#     server_name satryawiguna.me www.satryawiguna.me;
#
#     ssl_certificate     /etc/letsencrypt/live/satryawiguna.me/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/satryawiguna.me/privkey.pem;
#     ssl_protocols       TLSv1.2 TLSv1.3;
#     ssl_ciphers         HIGH:!aNULL:!MD5;
#
#     location / {
#         proxy_pass         http://web:3004;
#         proxy_http_version 1.1;
#         proxy_set_header   Upgrade           $http_upgrade;
#         proxy_set_header   Connection        "upgrade";
#         proxy_set_header   Host              $host;
#         proxy_set_header   X-Real-IP         $remote_addr;
#         proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
#         proxy_set_header   X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#         proxy_read_timeout 60s;
#     }
# }

# ── admin.satryawiguna.me (admin dashboard) ───────────────────────────────────
server {
    listen 80;
    server_name admin.satryawiguna.me;

    # return 301 https://$host$request_uri;

    location / {
        proxy_pass         http://admin:3005;
        proxy_http_version 1.1;
        proxy_set_header   Upgrade           $http_upgrade;
        proxy_set_header   Connection        "upgrade";
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 60s;
    }
}

# Uncomment and configure once you have SSL certificates for admin subdomain
# server {
#     listen 443 ssl http2;
#     server_name admin.satryawiguna.me;
#
#     ssl_certificate     /etc/letsencrypt/live/admin.satryawiguna.me/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/admin.satryawiguna.me/privkey.pem;
#     ssl_protocols       TLSv1.2 TLSv1.3;
#     ssl_ciphers         HIGH:!aNULL:!MD5;
#
#     location / {
#         proxy_pass         http://admin:3005;
#         proxy_http_version 1.1;
#         proxy_set_header   Upgrade           $http_upgrade;
#         proxy_set_header   Connection        "upgrade";
#         proxy_set_header   Host              $host;
#         proxy_set_header   X-Real-IP         $remote_addr;
#         proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
#         proxy_set_header   X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#         proxy_read_timeout 60s;
#     }
# }
